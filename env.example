# Database Configuration
MONGODB_URI=mongodb://localhost:27017/wilsons

# JWT Configuration (Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your-super-secret-jwt-key-here

# Socket.io Configuration
SOCKET_PORT=3001

# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: MongoDB Atlas (for production)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/wilsons?retryWrites=true&w=majority 